[project]
name = "3dbag-labs"
version = "0.1.0"
description = "The 3DBAG Labs documentation site"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-materialx>=1.0.0",
    "properdocs>=1.0.0",
]

[tool.uv]
package = false
